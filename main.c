#include <stdio.h>
#include "minilibx/mlx.h"

int main() {
    void *mlx;

    printf("Hello, World!\n");
    mlx = mlx_init();
//	mlx_new_window(mlx, 1000, 1000, "Title");
//	mlx_loop(mlx);
    return 0;
}
